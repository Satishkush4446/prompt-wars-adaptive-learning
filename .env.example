# Server-side environment variables (Vercel / serverless deployment)
# These are used by api/ai.ts and must NEVER be prefixed with VITE_
GEMINI_API_KEY=your_api_key_here
GEMINI_MODEL=gemini-3.5-flash
